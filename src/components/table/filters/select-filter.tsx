"use client";

//shadcn component
import {
  Select,
  SelectTrigger,
  SelectValue,
  SelectContent,
  SelectItem,
  SelectGroup,
  SelectLabel
} from "@/components/ui/select";

interface SelectFilterProps {
  options: string[];
}
export const SelectFilter = ({ options }: SelectFilterProps) => {};
